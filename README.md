
  # EaC Configuration Structure

  This is a code bundle for EaC Configuration Structure. The original project is available at https://www.figma.com/design/Tvhpek6o5BzBJm2g6IQDAu/EaC-Configuration-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  